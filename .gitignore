# Dependencies
node_modules/

# Build output
dist/
dist-app/
build/
*.tsbuildinfo

# Generated basemap assets (regenerate: scripts/fetch-basemap-assets.mjs + go-pmtiles extract)
app/public/basemap/

# Test / coverage
coverage/
.vitest/
test-results/
playwright-report/
blob-report/
playwright/.cache/
.playwright-mcp/

# Capacitor / native
android/
ios/
.gradle/
*.keystore
native/keystore.properties
# Release-attestation signing key — private half never committed (the .pub IS),
# same posture as the keystore: back it up out-of-band (docs/transparency/README.md).
native/release-signing-key
native/crypto-tests/.gradle/
native/crypto-tests/build/
compatibility/v1/kotlin-wraps.json

# Env & secrets
.env
.env.*
!.env.example
# Not a secret: the native (APK) build's proxy endpoints — see app/.env.native
!.env.native

# Editor / OS
.DS_Store
.idea/
.vscode/
*.log

# Claude Code / MCP / tooling artefacts
.claude/
CLAUDE.local.md
.superpowers/

# Local dev scratch (relay watchers, device screenshots) — never committed
scratchpad/

# Detailed market/competitor reports stay outside repository history.
docs/**/*competitor*
docs/private/

# Local deploy config — real host/user, never committed
deploy/deploy.local.sh
